name: fap_agent
description: Flutter Agent Protocol - WebSocket-based RPC protocol enabling AI-driven automation and testing of Flutter applications.
version: 1.0.0
homepage: https://github.com/mkritter3/flutter-autopilot
repository: https://github.com/mkritter3/flutter-autopilot

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  web_socket_channel: ^3.0.3
  json_rpc_2: ^4.0.0
  stream_channel: ^2.1.4
  flutter:
    sdk: flutter
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  flutter_test:
    sdk: flutter
